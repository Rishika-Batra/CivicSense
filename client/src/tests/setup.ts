/**
 * Vitest global test setup
 * Imports @testing-library/jest-dom to extend Vitest matchers with
 * DOM-specific assertions like toBeInTheDocument, toHaveValue, etc.
 */
import '@testing-library/jest-dom'
